import { AppRouter } from "./router/AppRouter";

const App = () => {
  return (
    <div style={{ cursor: `url('/navigation_small.png'), auto` }}>
      <AppRouter />
    </div>
  )
}

export default App;